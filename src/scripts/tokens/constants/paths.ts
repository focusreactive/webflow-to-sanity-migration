import { join } from "node:path";

export const RESPONSE_RELATIVE_PATH = join(".migration", "steps", "tokens", "response.json");

export const THEME_CSS_RELATIVE_PATH = join(".migration", "artifacts", "theme.css");
